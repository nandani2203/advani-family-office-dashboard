node_modules/
dist/
build/
.next/
out/
coverage/

.env
.env.local
.env.*.local
!.env.example

.vercel/
*.tsbuildinfo
next-env.d.ts

.DS_Store
Thumbs.db
*.log
npm-debug.log*


# Vendored AI-tool skill docs — third-party Prisma reference material pulled in
# by a skills installer, not part of this project. skills-lock.json records each
# one's source and hash, so any of these directories can be re-installed.
# Which assistant a developer uses is their own choice, so none of it is shared.
.agents/
.claude/
.windsurf/

# Assistant working notes — not part of the shipped project.
CLAUDE.md
claude.md
project.md
